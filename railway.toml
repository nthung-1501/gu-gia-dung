[build]
builder = "nixpacks"
buildCommand = "npm ci"

[deploy]
startCommand = "npx tsx src/workers/index.ts"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[[services]]
name = "workers"
