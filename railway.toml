[build]
builder = "nixpacks"
buildCommand = "echo 'Workers build complete'"

[deploy]
startCommand = "npx tsx src/workers/index.ts"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[[services]]
name = "workers"
